If FeedA 000 Then 1       // If feed mode is on, turn the pump down to the minimum (5% of PUMP_MAX). When feed mode ends, delay the switch by 0s (so none lol) before turning it back to max.
If Swx6_2 CLOSED Then OFF // If the tank high level sensor trips, turn the return pump off.