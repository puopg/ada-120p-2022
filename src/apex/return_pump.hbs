{{! Not in the wizard, but is in the actual code in the non fusion interface. }}
Fallback {{ apexVariables.RETURN_PUMP_DEFAULT_POWER_PERCENT }}
Set ON
{{! If feed mode is on, turn the pump down to the minimum. }}
{{! When feed mode ends, delay the switch by 0s (so none lol) before turning it back to max. }}
If FeedA 000 Then {{apexVariables.RETURN_PUMP_FEED_MODE_POWER_PERCENT }}
{{! If the tank high level sensor trips, turn the return pump off. }}
If {{apexModules.tank_water_high_optical_sensor.moduleName}} CLOSED Then OFF